# Taste
- Values backward compatibility when refactoring or adding new APIs: existing method signatures and callers must remain unchanged ("不改变现有方法的调用以保证兼容性"). Confidence: 0.9
- Prefers spatial-partitioning / indexing approaches (e.g., 16x16x16 cell grouping with bit-packed long keys) over full-list traversal for performance-sensitive entity queries; queries should only visit relevant partitions and then do precise checks (e.g., squared-distance). Confidence: 0.9
- Communicates in Chinese (requests and context written in Chinese). Confidence: 0.7
- Maintains a multi-project workspace where Lyra is the core library and Summoner is the consumer mod; when an API is changed, call-site updates belong in the consuming project (Summoner), and the user explicitly corrects the assistant when changes are applied to the wrong project. Confidence: 0.7
- Prefers radius-based queries that avoid full-list access, and chooses the query center/radius according to the caller's contextual semantics (e.g., owner position + search range for targeting; swept-OBB bounding-box center + coverage radius for collision). Confidence: 0.8
- Prefers passing filtering conditions as a Predicate/断言 into the query API (e.g., `getEntitiesInRadius(pos, radius, filter)`) rather than filtering manually in loops after retrieval. Confidence: 0.8
- Communicates in Chinese (requests and context written in Chinese). Confidence: 0.7
- When syncing code from a newer Minecraft-version branch into an older one where APIs differ across versions, asks the assistant to stop and ask targeted direction-confirming questions at ambiguous decision points rather than guessing ("遇到问题请提问确认改动方向"). Confidence: 0.9
- When cross-branch syncs hit genuine architecture divergences (not simple version-newer/older), prefers aligning the target branch to the newer branch's architecture as authoritative (full overwrite/port, e.g., replacing the interface-based weapon API with the data-component-based one) rather than keeping the older branch's divergent local implementation. Confidence: 0.8
- Commands in PowerShell and environment-checking must be robust on Windows: avoid cmd-incompatible pipes (`head`, `tail`), avoid mixing cmd/powershell in one invocation, and confirm a repo's identity/root (`git rev-parse --show-toplevel`, tracked vs. untracked files, ignore rules) before concluding a branch contains the wrong content — the assistant previously misread which git repository a directory belonged to. Confidence: 0.7
- For attachment-entity rendering, prefers real per-position world lighting (block+sky light, including block light emission, plus dynamic light) over a hardcoded constant full brightness, following the 26.2 branch's lighting behavior: each entity uses its own actual position light when not inside a solid block, otherwise falls back to the player's baseline light (from the light-probe position) to avoid rendering fully black. Confidence: 0.8
- Expects the computed light value to be threaded through the entire attachment-entity render API chain (dispatcher → abstract renderer render/modelModify → host-mod subclass renderers) as an explicit parameter matching the 26.2 branch's method signatures, so consumer-mod renderers (e.g., Summoner) can consume the real light instead of hardcoding a constant per renderer. Confidence: 0.7
- Favors rendering correctness over hand-rolled shortcuts when syncing model-rendering code from the newer branch (26.2 → 1.21.1): standalone models must be written with the complete entity vertex format including real per-quad normals (quad direction transformed through the pose's normal matrix — never hardcoded (0,0,1) or ignored), and baked models should be fetched on demand from Minecraft's ModelManager each render instead of pre-baking into a manual geometry cache. Confidence: 0.9
- Keeps shared public render-API signatures homomorphic across the two maintained MC-version branches (1.21.1 and 26.2) so external host mods (e.g., Summoner) can call the same API across versions with unchanged arguments — at most a low-cost parameter-type swap (e.g., swapping the model-key/buffer type) rather than rewriting call sites. Confidence: 0.8
- When syncing an entire project/mod from the newer MC-version branch to the older one, treats the newer branch (26.2) as the single authoritative source of truth and overwrites everything — business logic AND assets/textures (材质也需要同步), not just code — so the in-game presentation and behavior match exactly ("一切业务逻辑以26.2为准进行同步覆盖，确保表现效果一致"). Confidence: 0.95
- When the source branch's commit history contains abandoned development directions (e.g., marked arrows and summon crossbows later deprecated), only the newest state (branch HEAD) should be ported; abandoned features must not be resurrected from intermediate commits ("注意提交中包含已经废弃的开发方向…应该以最新的状态为准"). Confidence: 0.8
- Prefers to hand-fix the delicate, version-specific parts of a cross-version port himself (e.g., mixin injection points such as ItemEnchantmentsMixin), then asks the assistant to simply re-run the verification pipeline (compile → runData → client smoke) rather than continuing to iterate on those edits — "我手动修复了附魔mixin，重新尝试". Confidence: 0.6
- Because the user edits files directly in the working copy in parallel with the assistant, the assistant must re-read a file's current state before editing or judging it — files it previously wrote may have been changed/reverted externally (ItemEnchantmentsMixin kept reverting to a 26.2-only `DataComponentGetter` version while the assistant worked; likewise, LyraRarity was rewritten in-place on disk during the session, gaining a new `handler` and losing `tick()`/`stepDisco` between the assistant's own edits, prompting a re-read). Confidence: 0.8
 The user verifies ported work by actually play-testing the game in-world, then reports a terse bug list of observable divergences from the 26.2 branch (overlay depth-test transparency, tooltip layout and Shift-expand interaction, item use animation, movement slowdown) as the acceptance criteria — passing compileJava/runData/client-smoke alone is not treated as sufficient; every reported divergence must be fixed until the in-game presentation reproduces 26.2. Confidence: 0.7
- Summon-mark / target-indicator overlays must be see-through (可透视): alwaysVisible overlay textures are expected to render with a no-depth-test translucent pass (no depth test, color-only write) so marks stay visible through world geometry, rather than a depth-tested translucent render type that gets occluded — this semantic must be preserved across the 26.2 and 1.21.1 Lyra render APIs. Confidence: 0.7
- When reporting a remaining in-game parity gap ("效果仍然和26.2不一样，仔细检查"), the user supplies the exact target-version vanilla/decompiled source (e.g., 1.21.1 `ItemEnchantments.addToTooltip` and `TooltipFlag`) as ground truth; the assistant is expected to re-derive the fix precisely from that provided code — down to character-level output details such as ◇/◆ prefix spacing, colors, and which lines are emitted unconditionally vs. conditionally — rather than assuming API availability or approximating the reference behavior. Confidence: 0.6
- When adding/optimizing an effect on the 1.21.1 branch that the 26.2 branch already has, researches how the upstream third-party mod the 26.2 feature was derived from implements it on the matching MC version — by decompiling/reading the actual mod artifact (e.g., user pointed to the LambDynamicLights 4.8.11+1.21.1 jar for smooth-lighting research, noting the 26.2 branch's lighting system comes from LDL's 26.2 version) — rather than re-deriving the algorithm from scratch; the upstream mod is treated as the authoritative reference for both branches. Confidence: 0.7
- When migrating/refactoring an API (e.g., Lyra's item registration API), prefers to first run compilation to locate the error positions ("先编译看看错误位置") before starting edits, using the compile errors to drive the migration. Confidence: 0.6
- Avoids whole-method mixin takeover for mod compatibility: even when the functional result matches ("效果一致"), a tooltip/interception mixin must use the narrowly-scoped `@WrapWithCondition` pattern (26.2 style — intercepting the specific `Consumer.accept` call site with `@Local` captures, suppressing the original via returning false) rather than `@WrapMethod` wrapping the entire vanilla method, because `@WrapMethod` conflicts with other mods' mixin injections on the same method ("避免使用WrapMethod造成兼容性破坏，采用26.2mixin模式"). Confidence: 0.9
n模式"). Confidence: 0.9
